/** @type {import('next').NextConfig} */
const nextConfig = {
  // Set the port for both development and production
  experimental: {
    // Any experimental features can go here
  },
  // You can add other Next.js config options here
}

module.exports = nextConfig 